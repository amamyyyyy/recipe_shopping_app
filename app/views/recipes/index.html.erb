<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting"> 
        <%= "こんにちは、" %>
        <%= link_to "#{current_user.name}さん", root_path, class: :greeting__link%>
        <%= link_to '買い物リスト', shopping_list_recipes_path, class: 'btn btn-primary' %>
      </div>
    <%# // ログインしているときは上記を表示する %>
    <% end %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      <%= render partial: 'recipe', collection: @recipes %>
    </div>
  </div>
</main>
